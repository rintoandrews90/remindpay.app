# RemindPayWeb

Static marketing site (plain HTML/CSS, no build step) deployed to Cloudflare via Wrangler.

## Design

Before writing or changing any UI, read `DESIGN.md` and follow its design tokens and component recipes (colors, typography, spacing, radii). Use the token values from there instead of inventing new colors or fonts.
